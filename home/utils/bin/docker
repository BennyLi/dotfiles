#!/bin/sh

if [ "$1" == "tags" ] && [ $# -eq 2 ]; then
  image="$2"
  url="https://registry.hub.docker.com/v2/repositories/library/${image}/tags/"
  echo "Tags for image '$image':"
  curl -s "$url" | jq -r '."results"[]["name"]' | sed 's/^/\t/g'
  
else
  docker $@
fi
